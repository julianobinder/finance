import React from 'react';
import { Header as HeaderUI } from '@/components/ui/header';

export function Header() {
  return (
    <HeaderUI>
      <div className="flex items-center space-x-4">
        {/* Empty header - clean minimal design */}
      </div>
    </HeaderUI>
  );
}
